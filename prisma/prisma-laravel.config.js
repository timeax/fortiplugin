// prisma/prisma-laravel.config.js
module.exports = {
    tablePrefix: "scpl_",        // e.g. "tx_"
    tableSuffix: "",        // e.g. "_arch"
    // stubDir: "./prisma/stubs",
    prettier: true
    // migrate: { noEmit: false },
    // modeler: { noEmit: false }
};
