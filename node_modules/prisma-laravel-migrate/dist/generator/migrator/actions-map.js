/**
 * Map Prisma’s actions to the Laravel strings you defined in RelationshipOptions.
 */
const prismaToLaravelAction = {
    Cascade: 'cascade',
    Restrict: 'restrict',
    NoAction: 'no action',
    SetNull: 'set null',
    SetDefault: 'set default',
};
/**
 * Convert a Prisma referential action into the corresponding Laravel action.
 * Falls back to 'restrict' if you ever get an unexpected value.
 */
export function mapPrismaActionToLaravel(action) {
    return prismaToLaravelAction[action] ?? 'restrict';
}
//# sourceMappingURL=actions-map.js.map